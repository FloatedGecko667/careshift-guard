# CareShift Guard デプロイ手順

Oracle Cloud Infrastructure（OCI）上に構築する手順。
**検証環境（Always Free）** と **本番提案環境（有料）** を分けて記載する。

最終レポートのデモには検証環境を使う。
本番提案環境は費用の根拠を示すための構成であり、実際に構築する必要はない。

---

## 0. 事前準備

| 項目 | 内容 |
|---|---|
| OCIアカウント | Oracle Academy Cloud Program または個人のFree Tier |
| SSH鍵 | `ssh-keygen -t ed25519 -C "careshift"` で作成 |
| GitHubリポジトリ | 公開リポジトリとして作成済みであること |
| 所要時間 | 初回で約60〜90分 |

### Always Free 枠の現状（重要）

**2026年6月15日、Always Free の Ampere A1 割当が 4 OCPU・24GB から
2 OCPU・12GB へ半減されました。** 公式のアナウンスはなく、
ドキュメントの更新のみでした。

したがって検証環境は **2 OCPU / 12GB の1インスタンス** に収める。
この制約は本ソリューションの構成（3コンテナ）で問題なく満たせる。

またOracleのドキュメントには「既存リソースを一度終了した場合、
更新後の上限を超えるリソースを再作成できない可能性がある」と
記載されている。**インスタンスを不要に削除しないこと。**

---

## 1. インスタンスの作成

OCIコンソール → Compute → Instances → Create instance

| 設定項目 | 値 |
|---|---|
| Name | `careshift-demo` |
| Placement | 大阪リージョンの任意のAD |
| Image | Canonical Ubuntu 26.04（無い場合は 24.04。※後述） |
| Shape | Ampere → `VM.Standard.A1.Flex` |
| OCPU / Memory | **2 OCPU / 12 GB**（Always Free の上限） |
| Boot volume | 100 GB |
| VNIC | パブリックIPを割り当てる |
| SSH keys | 作成した公開鍵を貼る |

> **Ubuntu 26.04 のイメージが一覧に無い場合**
> 24.04 LTS を選ぶ。アプリとデータベースはすべてコンテナ内で動作するため、
> ホストOSの版はアプリの動作に一切影響しない。
> これはコンテナ技術を採用したことによる利点である。

### ネットワークの開放

VCN → Security Lists → Default Security List → Add Ingress Rules

| Source CIDR | Protocol | Destination Port |
|---|---|---|
| `0.0.0.0/0` | TCP | 80 |
| `0.0.0.0/0` | TCP | 443 |

**注意。** OCI はセキュリティリストとは別に、
インスタンスのOSファイアウォール（iptables）も既定で閉じている。
両方を開けないと外部から到達できない。ここでつまずく人が多い。

```bash
sudo iptables -I INPUT 6 -m state --state NEW -p tcp --dport 80 -j ACCEPT
sudo iptables -I INPUT 6 -m state --state NEW -p tcp --dport 443 -j ACCEPT
sudo netfilter-persistent save
```

---

## 2. ホストの準備

```bash
ssh ubuntu@<パブリックIP>

sudo apt-get update
sudo apt-get install -y ca-certificates curl git make

# Docker 公式リポジトリを追加（Ubuntu 同梱版は古い）
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
  -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc
echo "deb [arch=$(dpkg --print-architecture) \
signed-by=/etc/apt/keyrings/docker.asc] \
https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" \
  | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
sudo usermod -aG docker ubuntu
newgrp docker

docker --version
docker compose version
uname -m          # aarch64 と表示されること
```

**`aarch64` であることを必ず確認する。** Ampere A1 は Arm である。
x86_64 向けのイメージは動かない。本ソリューションが使う
Python 3.13 / PostgreSQL 18 / nginx の公式イメージはいずれも
arm64 に対応しているため、そのまま動く。

---

## 3. アプリケーションの配置

```bash
git clone https://github.com/FloatedGecko667/careshift-guard.git
cd careshift-guard

make init                    # .env を作成
```

`.env` を編集して秘密情報を設定する。**既定値のままでは本番起動できない**
（`app/config.py` が起動時に検出して失敗させる）。

```bash
# 強い値を生成する
openssl rand -base64 32      # POSTGRES_PASSWORD 用
openssl rand -hex 32         # SECRET_KEY 用

nano .env
```

```ini
POSTGRES_PASSWORD=<生成した値>
SECRET_KEY=<生成した値>
APP_ENV=production
SOLVER_TIME_LIMIT=10
SOLVER_WORKERS=2             # 割当OCPU数に合わせる
PUBLIC_HOSTNAME=<ドメインまたはIP>
# 利用者がブラウザで見る URL の基点。本番では必須。
# パスワード再設定リンクの絶対URLをここから組む。
# 未設定だと起動時に失敗する（推測に頼らせない設計）。
PUBLIC_BASE_URL=https://<ドメイン>
```

```bash
make vendor                  # htmx を取得（任意）
make up                      # ビルドと起動
```

初回のビルドは OR-Tools の取得に時間がかかる。
2 OCPU の環境で **5〜10分** 程度を見込む。

```bash
docker compose ps            # 3コンテナが healthy になること
make logs                    # 異常がないか確認
```

---

## 4. データベースの初期化

```bash
make migrate                 # db/schema.sql を Alembic 経由で適用
make seed                    # 人員配置基準ルールと初期データを投入
```

適用結果を確認する。

```bash
make shell
```

```sql
\dt                                    -- 12テーブルが並ぶこと
\dv                                    -- v_daily_fte があること
SELECT job_type, formula_type, base_fte, threshold_users, step_users
FROM staffing_rules WHERE service_type = 'day_service';
SELECT code, name, start_minute, end_minute, break_minutes, work_minutes
FROM shift_patterns ORDER BY display_order;
\q
```

`work_minutes` が生成列として自動算出されていることを確認する
（早番 420→960 休憩60 なら 480分）。

### 最初の管理者パスワードの設定

**この作業は最初の1回だけである。** 以降のアカウント発行と
パスワード再設定は画面（/accounts）から行う。
`seed.sql` の `password_hash` はプレースホルダなので、
ここだけは直接置き換える必要がある。

```bash
docker compose exec web python3 -c "
from app.security import hash_password
print(hash_password('ここに設定したいパスワード'))"
```

出力されたハッシュ値で更新する。

```bash
make shell
```

```sql
UPDATE users SET password_hash = '$argon2id$...'
WHERE email = 'admin@example.jp';
\q
```

以降の運用は画面で完結する。

| やること | 画面 |
|---|---|
| 職員のアカウントを作る | /accounts →「アカウントを作成する」→ 初回設定リンクを本人へ渡す |
| パスワードを忘れた職員の対応 | /accounts → 対象行の「再設定リンク発行」 |
| 退職者のアカウントを止める | /accounts → 対象行の「無効化」 |
| 自分のパスワードを変える | ヘッダの「パスワード変更」 |
| 誰がいつ確定したかを確認する | /audit |

---

## 5. 動作確認

### サーバ内部から

```bash
curl -s http://localhost:8080/healthz
# {"status":"ok","database":"up"}
```

`database` が `up` であることを確認する。`down` の場合は
`.env` の接続情報か db コンテナの状態を疑う。

### 外部から

ブラウザで `http://<パブリックIP>:8080/` を開く。

到達しない場合の切り分け順序は次のとおり。

1. `docker compose ps` で nginx が起動しているか
2. サーバ内から `curl localhost:8080` が通るか（通ればアプリは正常）
3. OCI のセキュリティリストで該当ポートを開けたか
4. `sudo iptables -L INPUT -n --line-numbers` でOS側が開いているか

**2が通って3か4で止まっているケースが最も多い。**

### スモークテスト

```bash
docker compose exec web python3 -m scripts.smoke_test
```

16項目すべてが OK になることを確認する。

---

## 6. TLS の設定（任意・デモでは省略可）

独自ドメインを持っている場合のみ。

```bash
sudo apt-get install -y certbot
sudo certbot certonly --standalone -d <ドメイン> \
  --agree-tos -m <メールアドレス> --non-interactive
```

`nginx/default.conf` に 443 の server ブロックを追加し、
証書のパスをマウントする。

本番提案環境では OCI Flexible Load Balancer で TLS を終端するため、
この作業は不要になる。

---

## 7. 最終レポート用のスクリーンショット取得

**今回の提出物は、この節の手順で取得した実測の画面である。**
検証は手元の arm64 Docker 上で行った。本番の Oracle Cloud（Ampere A1）は
同じ命令セット・同じイメージで動くため、第1〜6節の手順でそのまま移せる。

### 7.1 一括で取れるもの

```bash
make demo        # 起動・スキーマ適用・デモデータ投入・テスト・スモークテストまで
make evidence    # docs/images/raw/*.txt を読める画像に変換
make xlsximg     # 勤務形態一覧表の xlsx を LibreOffice 経由で画像化
```

`make demo` が `docs/images/raw/` に書き出す証跡と、
`make evidence` が生成する画像の対応は次のとおり。

| 生成される画像 | 元の出力 | 何の証拠か |
|---|---|---|
| `03_docker_compose_ps.png` | `docker compose ps` + `docker inspect` | 3コンテナが healthy であること |
| `04_env.png` | `uname -m` / `docker version` | arm64・aarch64 上で動いていること |
| `12_make_test.png` | コンテナ内 `pytest` | 236 passed, 2 skipped |
| `13_smoke.png` | `scripts/smoke_test.py` | HTTP 経由で全経路が期待どおり応答 |
| `14_database.png` | `psql` のスキーマ一覧 | PostgreSQL 18 に12テーブルが作られたこと |
| `15_seed.png` | `scripts/seed_demo.py` | デモデータの投入結果 |
| `17_excel_formula.png` | ホスト側 `pytest -k 数式` | Excel の数式を LibreOffice で実評価した2件 |
| `10_kinmu_keitai.png` | `make xlsximg` | 勤務形態一覧表（兼務が職種ごとに按分されている） |
| `11_daily_check.png` | `make xlsximg` | 日別の基準判定（月次適合・日別8日違反） |

コンテナ内のテストが 236 passed / 2 skipped になるのは、
Excel の数式を実評価する2件が LibreOffice を必要とするためである。
LibreOffice を顧客提供イメージに入れる理由がないので、
その2件だけホスト側で実行し `17_excel_formula.png` として別に証跡を残している。

### 7.2 ブラウザで撮るもの

ブラウザの幅を 1440px 程度にして撮る。
アドレスバーを含めると「どこで動いているか」が読み取れる。

| ファイル名 | 画面 | 撮影のポイント |
|---|---|---|
| `05_login.png` | ログイン画面 | 稼働環境（development / production）の表示を含める |
| `09_masters.png` | マスタ管理 | 兼務の列（管理者が生活相談員50%、機能訓練指導員が介護職員30%）と職員追加フォーム |
| `22_masters_list.png` | マスタ管理（一覧の先頭） | 職員数の見出しを含める |
| `08_requests_mobile.png` | 希望シフト | 画面幅を狭めて職員向けの見た目にする |
| `20_generate_form.png` | シフト表（未生成） | 平均利用者数の入力欄と算定式の説明文 |
| `06_schedule_ok.png` | シフト表（適合） | 緑の帯「すべての日で人員配置基準を満たしています」と求解結果 |
| `21_schedule_ok_fte.png` | シフト表（適合・最下段） | 職種別の常勤換算4行がすべて基準以上であること |
| `07_schedule_violation.png` | シフト表（違反） | 赤い帯と違反件数、無効化された「確定して公開」 |
| `18_violation_fte.png` | シフト表（違反・最下段） | 不足している職種の行が赤いこと |
| `19_violation_list.png` | 違反一覧 | 日付・職種・必要・実際・不足・対応の目安 |
| `16_publish_disabled.png` | 操作列の拡大 | 「確定して公開」が押せない状態 |
| `23_accounts.png` | アカウント管理 | 初回設定リンクの発行結果と一覧 |
| `24_audit.png` | 監査ログ | 日時・実行者・操作・対象・内容・接続元 |

### 7.2.1 狭い画面（職員が使う画面）

ブラウザの窓を狭めて **内側の幅を 720px 未満**にして撮る。
Chrome は窓の最小幅が 500px 程度あるため、`innerWidth` を確かめること。

```js
// 開発者ツールのコンソールで確認する
[innerWidth, getComputedStyle(document.querySelector('nav.tabbar')).display]
// → [606, "flex"] のように tabbar が flex になっていれば狭い画面の表示
```

| ファイル名 | 画面 | 撮影のポイント |
|---|---|---|
| `08_requests_mobile.png` | 希望シフト | カレンダーと下端の固定タブ。希望休が色と文字で示されていること |
| `25_my_shift_mobile.png` | シフト表（職員） | 「〇〇さんのシフト」の一覧。日付・勤務区分・時間帯・実働時間 |

`25_my_shift_mobile.png` は **確定済みのシフトが必要**である。
職員には確定前のシフトを見せない仕様のため、先に管理者が
「確定して公開」を押しておく（違反0件でなければ押せない）。

違反状態を再現するには、職員構成を変えずに平均利用者数を上げるのが早い。
介護職員の必要常勤換算数は「利用者15人まで1以上、超過分を5で除した数を加える」
であるため、平均利用者60名なら必要 10.0 になる。
本リポジトリのデモデータ（職員20名）では日単位で最大 8.6 程度しか用意できず、
20日分が不足する。

### 7.3 動画とスライドに反映する

```bash
make pv        # docs/紹介動画_CareShift_Guard.mp4 と .srt を生成
make slides    # docs/発表スライド_CareShift_Guard.pptx に画像と動画を埋め込む
```

`make slides` は不足している画像を一覧で表示する。
「すべてのスクリーンショットが埋め込まれました」と出れば完了である。

---

## 8. デモ後の停止について

**インスタンスを削除しないこと。** Always Free の上限が縮小された経緯があり、
一度終了すると同じ構成で再作成できない可能性がある。

費用をかけずに止めたい場合はインスタンスを Stop する。
Always Free の枠内であれば、起動していても課金されない。

---

## 付録A. 本番提案環境の構成（費用根拠）

顧客に提供する構成。Pay As You Go 契約を前提とする。

| 項目 | 構成 | 月額（税別・165円/USD） |
|---|---|---|
| VM1（nginx + web） | Ampere A1 2 OCPU / 12GB | 4,577円 |
| VM2（PostgreSQL 18） | Ampere A1 2 OCPU / 16GB | 5,300円 |
| ブロックボリューム | 100GB Balanced | 701円 |
| オブジェクトストレージ | 100GB | 421円 |
| ロードバランサ | 10Mbps | 0円（有料アカウントは1台目・10Mbpsまで無償） |
| 送信データ転送 | — | 0円（全リージョンで無償） |
| 監視 | OCI Monitoring / Alarms | 0円（標準機能） |
| **合計** | **50テナント収容** | **10,999円** |

1テナントあたり **220円/月**。月額利用料 16,500円に対する
インフラ原価の比率は 1.3 パーセントである。

### 本番環境で無料枠を使わない理由

無料枠には SLA がなく、予告なく縮小され得る。
実際に2026年6月、Ampere A1 の割当が半減された。

「補助金を前提としない」という条件と同じ論理で、
**無料枠を前提とした収益計画も成立しない**と判断した。
検証環境は無料枠、顧客に提供する本番環境は有料契約と明確に分離する。

---

## 付録B. つまずきやすい点

| 症状 | 原因 | 対処 |
|---|---|---|
| 外部からアクセスできない | OSファイアウォール | 手順1の iptables を実行 |
| ビルドが極端に遅い | 2 OCPU で OR-Tools を取得 | 待つ。5〜10分は正常 |
| `database: down` | 接続情報の不一致 | `.env` と compose の環境変数を確認 |
| 起動直後に web が落ちる | SECRET_KEY が既定値のまま | `.env` を修正。設計上の意図的な失敗 |
| exec format error | x86_64 向けイメージ | `uname -m` で aarch64 を確認。公式イメージを使う |
| メモリ不足でビルドが失敗 | 12GB を超過 | `docker builder prune` の後に再試行 |
| シフト生成が10秒で終わらない | 職員数が多い | `SOLVER_TIME_LIMIT` を延ばす。上限到達でも最良解が返る |
| **db が Restarting を繰り返す**<br>ログに `unused mount/volume` | PostgreSQL 18 でデータの置き場所が変わった | ボリュームは `/var/lib/postgresql` にマウントする。`/var/lib/postgresql/data` は17までの位置 |
| **生成すると 504 Gateway Time-out**<br>求解自体は成功している | nginx の `proxy_read_timeout` がソルバーの制限時間より短い | `/schedules/generate` だけ 330 秒にしてある。`SOLVER_TIME_LIMIT` を上限300秒より上げるなら nginx も同時に伸ばす |
| **nginx の設定変更が反映されない**<br>`nginx -t` がファイルなしと言う | 単一ファイルのバインドマウントは編集で inode が入れ替わると見えなくなる | `docker compose restart nginx`。reload では直らない |
| **コンテナ内のテストが3ファイルまるごと収集エラー**<br>`requires the httpx2 package` | `starlette.testclient` の依存が httpx から httpx2 に移った | `pyproject.toml` の dev に `httpx2` があることを確認。手元に httpx があると気づけない |
| **本番で起動しない**<br>`PUBLIC_BASE_URL を設定してください` | パスワード再設定リンクの絶対URLを推測に頼らせない設計 | `.env` に `PUBLIC_BASE_URL=https://ドメイン` を設定する |
| **再設定リンクのポートが落ちる**<br>`http://localhost/password/reset?...` | nginx が `Host $host` を渡していた（$host はポートを含まない） | `proxy_set_header Host $http_host;` にする。加えて `PUBLIC_BASE_URL` を設定すれば推測しない |
| **パスワードを変えたのに古い Cookie で入れる** | 署名Cookieはサーバに状態を持たない | `users.session_epoch` を要求ごとに照合している。0002 を適用したか確認する |
