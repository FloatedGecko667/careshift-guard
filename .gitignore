# ---------------------------------------------------------------------
# 秘密情報 ── 絶対にコミットしない
# ---------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json

# ---------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# ---------------------------------------------------------------------
# テストおよび開発中の生成物
#   docs/samples/ 配下は意図的にコミットする（レビュー用のサンプル出力）。
#
#   注意: `*.html` のような広いパターンを使うと、
#   app/templates/partials/ のようなサブディレクトリのテンプレートまで
#   除外してしまう（`!app/templates/*.html` は1階層しか効かない）。
#   実際にこれで request_row.html が漏れ、デプロイ時にテンプレート不足で
#   アプリが壊れる状態になった。
#   そのため生成物は「置かれる場所」で限定して除外する。
# ---------------------------------------------------------------------
/*.xlsx
/*.html
検証用_*
/preview_*.html

# LibreOffice の一時ファイル
.~lock.*#
~$*

# ---------------------------------------------------------------------
# コンテナ・DB
# ---------------------------------------------------------------------
pgdata/
*.sqlite3

# ---------------------------------------------------------------------
# エディタ・OS
# ---------------------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------
# 個人の作業ファイル（テンプレート原本・旧版など）
#   リポジトリには含めない
# ---------------------------------------------------------------------
_work/

# ---------------------------------------------------------------------
# 取得物（make vendor で配置する）
# ---------------------------------------------------------------------
app/static/htmx.min.js
